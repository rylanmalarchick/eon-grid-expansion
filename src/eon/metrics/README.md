# Metrics

- `objective.py` — canonical objective: weighted sum of total congestion + build cost
  (`canonical_congestion_mw`, `aggregate_metrics`, `ScenarioMetrics`).

## Planned (not yet implemented)

- `hardness.py` — optimality-gap, LP-IP gap, B&B node count summaries.
- `quantum_quality.py` — approximation ratio, success probability, circuit depth budget.
